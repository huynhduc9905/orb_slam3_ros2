| Feature/Bug | Description | GitHub Issue/PR | Status | Date | Reported Behavior | Upstream Workaround / Fix | Applicability / Confidence |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **GBA Infinite Loop (Freeze)** | Infinite loop in `RunGlobalBundleAdjustment` when updating keyframes after map load / loop closure. A keyframe's descendent can become its parent, causing `lpKFtoCheck` to loop infinitely. | [Issue #911](https://github.com/UZ-SLAMLab/ORB_SLAM3/issues/911) | Closed | 2024-06-27 | SLAM system freezes with messages "Local Mapping STOP". The GBA thread gets stuck and never releases the local mapping mutex. | **Fix:** Use an `unordered_set<KeyFrame*> visitedKF` to prevent adding already visited KeyFrames or their children to `lpKFtoCheck`. | **High** - Explains the freeze/deadlock during stereo loop closure GBA. |
| **Missed / False Loop Detections** | Bugs in loop detection logic where iterator does not advance if a candidate keyframe (`pKFi`) is bad, causing loop closure to get stuck or fail. | [PR #745](https://github.com/UZ-SLAMLab/ORB_SLAM3/pull/745) | Open | 2023-04-23 | Loop detection gets stuck and fails to close loops. | **Fix:** Modify `KeyFrameDatabase.cc` to ensure iterators are updated even when a KeyFrame is marked as bad. | **Medium/High** - Directly impacts loop detection stability. |
| **"BAD LOOP!!!" Error** | Frequent "BAD LOOP!!!" rejections during stereo-inertial loop closure due to strict, hardcoded thresholds for `phi` (rotation error). | [Issue #394](https://github.com/UZ-SLAMLab/ORB_SLAM3/issues/394) | Open | 2021-09-26 | Loop closure is rejected as "BAD LOOP!!!" because rotation errors slightly exceed hardcoded thresholds (e.g. 0.008 rad). | None official. Users manually relax the thresholds or disable the loop closure thread as a workaround. | **High** - Explains why stereo/VIO setups frequently miss valid loops. |
| **Map Merging "Teleport"** | When two maps overlap and merge, the current map merges into the older map ("down"), causing the robot to "teleport" without warning. | [Issue #718](https://github.com/UZ-SLAMLab/ORB_SLAM3/issues/718) | Open | 2023-02-24 | Sudden localization jumps when map merging occurs. | None official. Suggestion to merge the older map into the current map ("up") instead. | **Medium** - Explains undesirable behavior during map merges. |
| **Cannot Correct Loop (Mono/Stereo)** | Fails to correct loops even when successfully running. System doesn't break but misses loop closures entirely. | [Issue #57](https://github.com/UZ-SLAMLab/ORB_SLAM3/issues/57) | Closed | 2020-08-13 | Loops are visually evident but the system ignores or fails to correct them. | Ensure candidate images have similar points of view and enough map points exist for descriptor matching. | **Medium** - Behavior linked to descriptor constraints and viewing angles. |
